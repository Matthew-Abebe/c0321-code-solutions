/* exported getPropertyValue */

function getPropertyValue(object, string) {
  object = {
    name: '',
    occupation: '',
    birthplace: ''
  };

  var key = '';
  var output = object[key];

  return output;
}
